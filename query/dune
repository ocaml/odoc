; This library give functions to meaningfully query the database datastructure
; defined in `db`.

(library
 (name query)
 (libraries lwt re db))

(menhir
 (modules type_parser)
 (flags --explain))

(ocamllex type_lexer)
