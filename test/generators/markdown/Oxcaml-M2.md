
# Module `Oxcaml.M2`

Module with `portable` modality. The modality is applied to all value members of `M2`.

```ocaml
val x : int
```
```ocaml
val f : string -> bool
```
```ocaml
type s = {
  a : int;
}
```