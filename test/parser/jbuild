(jbuild_version 1)

(executable
 ((name test)
  (libraries (alcotest parser_ print))))

(alias
 ((name tester)
  (deps (test.exe))))

(alias
 ((name runtest)
  (deps ((alias tester)))
  (action (run ${exe:test.exe}))))
